# Workstream 03: Post-Roadmap Hardening

Goal:
- continue hardening beyond the current roadmap once the external validation gaps are closed

Candidate tasks:
- [ ] move reconciliation execution from the inline API path into the worker/queue path
- [ ] replace the current caller-principal placeholder with the full auth model from the security plan
- [ ] extend metrics from counters to richer reconciliation latency and state gauges
- [ ] expand the filing evidence set with benchmark output snapshots
